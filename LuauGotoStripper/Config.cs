namespace LuauGotoStripper;

public static class Config
{
    // Put your bot token here.
    public const string Token = "Tokenhere";

    // Optional: If set to a guild id, slash commands will register only in that guild (faster updates).
    // Leave blank for global registration (can take longer to appear).
    public const string Guild = "";
}

